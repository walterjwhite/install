#!/bin/sh

clear
warn "$1: $2"

_DEFAULT=$(echo $2 | awk -F'/' {'print$1'})

read _PROCEED
clear

if [ -z "$_PROCEED" ]; then
	warn "No user input entered, using default ($_DEFAULT)"
	_PROCEED=$_DEFAULT
fi

if [ $_PROCEED = "n" ]; then
	exit 1
fi

exit 0
